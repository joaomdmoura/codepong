<%- model_class = Player -%>
<div class="page-header">
  <h1>Ranking</h1>
  <hr>
  <pre>Clique em <span class="btn btn-mini">+resultado</span> na linha do seu nome para adicionar uma partida.</pre>
  <pre>Clique no nome de qualquer jogador um dos para ver o histórico de partidas.</pre>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>&nbsp;</th>
      <th>Nome</th>
      <th>R</th>
      <th>V</th>
      <th>D</th>
      <th>&nbsp;</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @players.each_with_index do |player, index| %>
      <tr>
        <td><%= (index+1) %></td>
        <td><%= link_to player.name, player_macthes_path(player) %></td>
        <td><%= (player.rating == 9.999999999621423e-06) ? 0 : '%.4f' % player.rating  %></td>
        <td><%= player.wins %></td>
        <td><%= player.losses %></td>
        <td>
          <%= link_to t('.challenge'),
            challenge_player_path(player), 
            :class => 'btn btn-mini btn-danger' %>
        </td>
        <td>
          <%= link_to '+resultado',
            player_match_path(player), 
            :class => 'btn btn-mini' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

